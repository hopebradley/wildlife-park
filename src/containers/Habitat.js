import Animal from '../components/Animal'

const Habitat = (props) => {

    // const habitatName = props.name
    // console.log(habitatName)

    // const animals = props.animals

    // const habitatAnimals = animals.filter(a => a.habitat_id = )

    return (
        <div>
        </div>
    )
}

export default Habitat
